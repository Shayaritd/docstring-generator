# FastAPI inference service — versions current as of July 2026
fastapi==0.140.2
uvicorn[standard]==0.35.0
pydantic>=2.13.0
python-multipart>=0.0.9

# Reuses the training-stack model loading code (Day 3) — same pins as training/requirements.txt
torch>=2.4.0
transformers==5.13.0
peft==5.8.1
bitsandbytes==5.14.0

# Additional for logging
python-json-logger>=2.0.0
